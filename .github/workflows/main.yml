name: hello-world
on: push
jobs:
  build:
    runs-on: ubuntu-latest
      - uses: actions/checkout@v2
      - run: make setup
  test-lint:
    needs: build
    runs-on: ubuntu-latest
      - run: make lint
      - run: make test

